torch>=2.4.0
transformers>=4.51.0
accelerate>=1.0.0
sentencepiece>=0.2.0
protobuf>=4.25.0
safetensors>=0.4.0
numpy>=1.26.0
tqdm>=4.66.0
psutil>=5.9.0
# optional but recommended on NVIDIA:
# flash-attn  # install separately if build succeeds
